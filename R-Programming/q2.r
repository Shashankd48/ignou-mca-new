# Create a vector of first 50 even numbers, starting from 2. Also create a vector having values
# 30 down to 1, as 30, 29, …,1

# Create a vector of first 50 even numbers starting from 2
even_numbers <- seq(from = 2, by = 2, length.out = 50)

# Create a vector of values from 30 down to 1
values_down_to_1 <- 30:1

# Print the vectors
print(even_numbers)
print(values_down_to_1)
